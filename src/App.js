var React = require('react');

var App = React.createClass({
  render() {
    return (
      <div>
        <h1>Open Library</h1>
        <h3> Books </h3>
      </div>
    );
  }
});

module.exports = App;
